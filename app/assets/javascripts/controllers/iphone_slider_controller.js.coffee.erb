@app.controller "iphoneSliderCtrl", ["$scope", ($scope) ->
  $scope.intervalTimer = 5000
  $scope.slides = []

  $scope.slides.push {
    image: '<%= asset_path 'screenshots/subscribe.png' %>'
  }

]
